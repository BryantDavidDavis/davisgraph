/*

$(document).ready( function() {
	var img = $('.photo-show-img');
if (img.height() >= img.width()) {
	img.css("min-height", "90%");
	alert('we have a long load, and the height is:' + img.height());
} else {
	img.css("min-width", "90%");
	alert('we have a wide load');
}	
});
*/
